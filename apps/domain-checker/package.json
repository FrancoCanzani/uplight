{
  "name": "domain-checker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o bin/domain-checker main.go",
    "check-types": "go vet ./...",
    "deploy": "flyctl deploy",
    "dev": "go run main.go",
    "lint": "go fmt ./... && go vet ./..."
  }
}
